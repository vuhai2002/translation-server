# pnpm v11 reads its settings (minimumReleaseAge, strictPeerDependencies,
# auditLevel, onlyBuiltDependencies / ignoredBuiltDependencies, ...) from
# pnpm-workspace.yaml -- NOT from this file. See pnpm-workspace.yaml for the
# supply-chain hardening config.
#
# Keep .npmrc for npm-compatible registry / auth settings only (none needed yet).
